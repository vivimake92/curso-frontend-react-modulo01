const makes = [
    "batom", 
    "Gloss", 
    "Base", 
    "Corretivo", 
    "Rímel"
]

makes.forEach((maquiagem) => {
    console.log(`Comprar: ${maquiagem}`)
})

makes.push("Pó compacto")

console.log(makes)